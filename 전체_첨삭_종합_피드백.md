# 전체 첨삭 종합 피드백

이 문서는 저장소에 있는 `첨삭.md` 13개를 모두 읽고, 여러 문제에서 반복해서 나타난 강점과 보완점을 정리한 종합 피드백입니다.

## 한 줄 총평

알고리즘의 큰 방향은 빠르게 잡고 핵심 점화식·탐색 구조도 잘 구현하지만, **초기 상태와 불변식, 경계 조건, 입력의 숨은 가정, 출력 형식을 마지막에 검증하는 습관**을 더 단단하게 만들 필요가 있습니다.

즉, 지금 가장 필요한 것은 새로운 알고리즘을 많이 외우는 것이 아니라, 이미 알고 있는 알고리즘이 모든 입력에서 정답이 되는지 끝까지 확인하는 능력입니다.

## 1. 잘하고 있는 점

### 1-1. 알고리즘의 큰 방향을 대부분 올바르게 선택합니다

다음 문제들에서 핵심 방향은 잘 잡았습니다.

- 영역 탐색에서 `deque`, 방향 벡터, 방문 처리를 사용했습니다.
- 미로 최단 거리에서 현재 칸의 거리 `+ 1`을 다음 칸에 기록했습니다.
- `1로 만들기`, `개미 전사`, `타일 만들기`, `효율적인 화폐 구성`에서 DP를 선택했습니다.
- 위상 정렬에서 진입 차수와 큐를 사용했습니다.
- 크루스칼에서 간선을 가중치 순으로 정렬하고 Union-Find로 사이클을 검사했습니다.
- 다익스트라에서 최소 힙과 오래된 거리 무시 방식을 사용했습니다.
- 떡 자르기에서 “가능 여부”의 단조성을 찾아 이진 탐색했습니다.

이것은 문제의 키워드를 보고 적절한 알고리즘 후보를 떠올리는 능력이 이미 형성되고 있다는 뜻입니다.

### 1-2. 핵심 로직이 맞는 경우가 많습니다

DP의 점화식 방향, BFS의 거리 누적, 다익스트라의 완화, 위상 정렬의 진입 차수 감소, 이진 탐색의 탐색 방향 등은 대체로 맞았습니다.

특히 다음 부분은 좋은 습관입니다.

- 큐에 넣는 시점에 방문 처리하기
- `deque.popleft()`로 큐를 처리하기
- 힙에서 꺼낸 값이 최신 거리인지 비교하기
- 이진 탐색에서 조건을 만족한 현재 값을 답 후보로 저장하기
- DP에서 가능한 선택을 `min` 또는 `max`로 비교하기

### 1-3. 틀린 원인을 찾아 수정하는 방향도 점점 좋아지고 있습니다

미로 문제에서는 처음보다 거리 배열을 직접 갱신하는 구조로 개선했고, 최소 힙 문제에서는 `heappush`의 부모 인덱스를 수정했습니다. 단순히 코드를 바꾸는 데서 끝나지 않고, 자료구조의 동작 원리를 이해하려고 한 점은 좋습니다.

## 2. 가장 우선적으로 발전해야 할 부분

### 2-1. DP 초기 상태를 점화식보다 먼저 검증하기

가장 반복해서 나타난 문제입니다.

- `1로 만들기`: 이미 목표인 `X = 1`의 연산 횟수를 `0`이 아닌 `1`로 초기화했습니다.
- `개미 전사`: `dp[1]`을 `arr[1]`로만 두어 `arr[0]`을 선택하는 경우를 잃었습니다.
- `타일 만들기`: 점화식은 맞지만 `dp[0]`, `dp[1]`, `dp[2]`의 의미와 사용 여부를 더 명확히 정리할 필요가 있습니다.
- `효율적인 화폐 구성`: 동전 하나를 미리 `dp[coin] = 1`로 넣다가 목표 금액보다 큰 동전에서 인덱스 오류가 발생했습니다.

DP를 풀 때는 코드를 쓰기 전에 다음 세 문장을 반드시 적어 보세요.

```text
dp[i]의 의미 = 
가장 작은 상태 = 
현재 상태를 계산하는 데 필요한 이전 상태 = 
```

그 다음 최소 입력을 직접 대입합니다.

```text
n = 1
n = 2
```

초기값이 상태 정의와 맞지 않으면 점화식이 아무리 정확해도 전체 답이 틀립니다. 현재는 점화식보다 초기화에서 더 자주 실수가 발생하므로, DP 문제에서는 초기값 검증을 가장 먼저 해야 합니다.

### 2-2. 자료구조의 불변식을 코드의 모든 줄에서 지키기

현재 가장 중요한 공통 약점입니다. 불변식은 “탐색이나 갱신 중에도 항상 참이어야 하는 규칙”입니다.

- BFS: 방문한 정점은 큐에 다시 넣지 않는다.
- Union-Find: 부모를 바꿀 때는 원래 정점이 아니라 루트의 부모를 바꾼다.
- 최소 힙: 모든 부모 값이 자식 값보다 작거나 같다.
- 위상 정렬: 진입 차수가 0인 정점만 큐에 넣고, 결과에 모든 정점이 들어왔는지 확인한다.
- 플로이드–워셜: 중간 정점 `k`를 허용한 뒤의 최단 거리라는 의미를 유지하며 `k → i → j` 순서를 지킨다.

이 부분은 다음 문제들에서 직접 드러났습니다.

- 미로 탈출: 이미 방문한 칸을 다시 큐에 넣어 중복 탐색이 발생했습니다.
- 크루스칼: `parents[a]`, `parents[b]`를 변경해 루트 연결이 깨졌습니다.
- 최소 힙: `heappop`에서 한 단계만 내려가고, 자식 존재 여부도 충분히 확인하지 않았습니다.
- 위상 정렬: 사이클이 있을 때 결과에 일부 정점만 들어온 상태를 정상 결과처럼 반환할 수 있습니다.

코드를 작성할 때 각 자료구조에 대해 다음 질문을 한 줄로 적어 두면 좋습니다.

```text
이 자료구조가 항상 유지해야 하는 규칙은 무엇인가?
어떤 순간에 그 규칙을 복구하는가?
```

### 2-3. 샘플 통과와 정답을 구분하기

여러 첨삭에서 샘플 입력은 맞았지만 작은 반례에서 오답이 확인되었습니다.

- `개미 전사`: 예제에서는 8이지만 `100 0 0 200`에서 200을 반환합니다.
- `크루스칼`: 샘플에서는 159가 나오지만 루트가 아닌 부모 갱신 때문에 다른 입력에서 42를 반환합니다.
- `1로 만들기`: 26은 맞지만 `X = 1`에서 틀립니다.
- `효율적인 화폐 구성`: 예제는 맞지만 목표보다 큰 동전이 있으면 `IndexError`가 발생합니다.
- `미로 탈출`: 작은 미로에서는 결과가 나와도 큐 중복으로 큰 입력에서 비효율적입니다.

앞으로는 샘플을 확인한 뒤 바로 다음 네 종류를 추가하세요.

1. 가장 작은 입력
2. 답이 없는 입력
3. 코드가 특별히 처리하는 조건을 깨는 입력
4. 선택지가 서로 충돌하는 입력

반례는 랜덤하게 만들기보다, 의심되는 코드 한 줄이 실패하도록 설계하면 빠르게 찾을 수 있습니다.

예를 들어 초기화가 의심되면 첫 두 원소를 극단적으로 만듭니다.

```text
100 0 0 200
```

방문 처리가 의심되면 왕복 가능한 작은 그래프나 꽉 찬 3 x 3 격자를 만듭니다.

## 3. 문제 유형별 현재 상태

| 유형 | 현재 강점 | 반복해서 확인할 부분 |
|---|---|---|
| DP | 상태 전이 방향과 `min`/`max` 선택은 대체로 정확함 | 상태 정의, 초기값, 최소 입력, 불가능한 상태 |
| DFS/BFS | 큐·방향 벡터·거리 누적 구조를 이해함 | 방문 처리 시점, 중복 삽입, BFS와 DFS 선택 기준 |
| 이진 탐색 | 단조성을 찾아 최대 가능한 값을 탐색함 | 구간의 포함 여부, `mid` 제거 시점, 목표량 0 같은 경계 |
| 위상 정렬 | 진입 차수 기반 Kahn 방식이 맞음 | 사이클, 결과 정점 수, 출력 형식 |
| Union-Find | `find`와 경로 압축의 개념을 사용함 | 항상 루트끼리 union하기, 연결 여부 확인 |
| 최단 경로 | 다익스트라·플로이드–워셜의 기본 구조를 구현함 | 방향/무방향, 음이 아닌 가중치, INF, 도달 불가 |
| 힙 | 삽입의 위쪽 이동 원리는 이해함 | 삭제의 반복적인 아래쪽 이동, 빈 힙, 실제 큐 연결 |

## 4. 입력 조건과 출력 형식을 더 꼼꼼히 보기

알고리즘 자체보다 문제의 계약을 놓친 사례가 여러 번 있었습니다.

### 입력에서 확인할 것

- 그래프 간선이 방향인가, 무방향인가?
- 시작 정점이나 고립된 정점이 입력에 등장하지 않을 수 있는가?
- 중복 간선이 가능한가?
- 목표 금액보다 큰 동전이 가능한가?
- `N = 1`, `X = 1`, 목표량 `0`이 가능한가?
- 경로가 존재하지 않을 때 어떤 값을 출력하는가?
- 간선 가중치가 모두 같은가, 음수일 수 있는가?
- 결과를 특정 수로 나눈 나머지를 출력해야 하는가?

### 출력에서 확인할 것

계산 결과가 맞아도 출력 형식이 다르면 오답입니다.

- 리스트를 그대로 출력해도 되는가?
- 행렬을 공백으로 출력해야 하는가?
- 도달 불가능한 곳을 `INF`, `0`, `-1` 중 무엇으로 출력하는가?
- `solution()`이 값을 반환하는가, 함수 내부에서 출력하는가?
- `print(solution())`에서 `None`이 출력될 가능성은 없는가?

다익스트라와 플로이드–워셜 첨삭에서 리스트 괄호·쉼표·`None` 출력 문제가 반복해서 지적되었습니다. 제출 직전에는 계산 로직과 별개로 출력 한 줄만 따로 확인하는 습관이 필요합니다.

## 5. 알고리즘 선택은 맞지만, 더 단순한 선택지를 비교하기

현재 풀이가 틀리지 않더라도 문제 조건에 비해 무거운 알고리즘을 선택하는 경우가 있습니다.

- 모든 간선 비용이 1인 최단 경로 → 다익스트라보다 BFS 우선
- 정점 수가 작고 모든 쌍의 거리가 필요함 → 플로이드–워셜 고려
- 연결 요소 개수 세기 → BFS도 가능하지만 재귀 깊이를 확인한 DFS가 더 간단할 수 있음
- 동전의 규칙이 일반적이지 않음 → 그리디를 바로 믿지 말고 DP 검토

이것은 “더 복잡한 알고리즘을 쓰면 안 된다”는 뜻이 아닙니다. 다음 세 질문으로 가장 단순한 충분조건을 먼저 찾으라는 뜻입니다.

```text
간선 비용이 모두 같은가?
정점 수가 작아서 전체 표를 만들어도 되는가?
같은 상태를 한 번만 계산하거나 방문하면 되는가?
```

## 6. 코드 작성 습관에서 보이는 개선점

### 6-1. 의미를 흐리는 특별 초기화를 줄이기

다음과 같은 코드는 당장은 동작할 수 있지만 상태의 의미를 흐립니다.

```python
results = {0: 0, 1: 1, 2: 1, 3: 1, 5: 1}
dp[coin] = 1
dp[i - 1] * dp[1] + dp[i - 2] * (dp[2] - 1)
```

가능하면 기본 상태 하나와 일반적인 점화식으로 통일하세요.

```python
dp[1] = 0
dp[value] = min(dp[value], dp[value - coin] + 1)
dp[i] = dp[i - 1] + 2 * dp[i - 2]
```

상태의 의미가 코드에 그대로 드러나면 경계 조건과 증명이 쉬워집니다.

### 6-2. 사용하지 않는 코드와 실제 사용하지 않는 구현을 정리하기

최소 힙을 직접 구현했지만 다익스트라에서는 `heapq`를 사용한 사례처럼, “연습용 구현”과 “실제 풀이에서 사용하는 구현”이 섞인 경우가 있습니다. 연습 목적이라면 두 부분을 분리하고, 제출 코드라면 실제로 사용하는 코드만 남기는 편이 좋습니다.

사용하지 않는 그래프, 함수, import도 정리하세요. 코드를 짧게 만드는 목적보다, 핵심 상태와 자료구조가 무엇인지 한눈에 보이게 만드는 것이 중요합니다.

### 6-3. 로컬 테스트용 입력과 제출용 입력을 구분하기

`input.txt`를 자동으로 열도록 한 방식은 로컬 테스트에는 편리합니다. 다만 제출 전에는 표준 입력을 사용하는 코드와 섞여 있지 않은지 확인해야 합니다. 로컬에서만 동작하는 설정이 정답 코드의 일부라고 착각하지 않도록 테스트용 처리와 풀이 로직을 구분해 두세요.

## 7. 앞으로 문제를 풀 때 사용할 검토 순서

### 1단계: 문제의 계약 표시하기

문제에서 다음 네 가지를 코드 옆에 적습니다.

```text
입력 크기:
그래프 방향/가중치 또는 선택 규칙:
도달 불가/불가능한 경우의 출력:
출력 형식:
```

### 2단계: 상태 또는 불변식 한 문장으로 쓰기

예시는 다음과 같습니다.

```text
dp[i] = i까지 고려한 최댓값
visited에 들어간 정점은 다시 큐에 넣지 않는다
parent[x]는 x가 속한 집합의 대표를 향한다
heap의 모든 부모는 자식보다 작거나 같다
```

### 3단계: 최소 입력을 손으로 계산하기

반드시 `1`, `2`, 빈 관계, 고립 정점, 도달 불가 상태를 먼저 확인합니다. 이 단계에서 `arr[1]`, `dp[coin]`, `graphs[node]` 같은 위험한 접근이 드러납니다.

### 4단계: 점화식·갱신 조건이 모든 경우를 포함하는지 확인하기

DP라면 선택하거나 선택하지 않는 경우가 모두 있는지, 타일이라면 경우가 겹치지 않는지 확인합니다. 그래프라면 이미 방문한 상태를 다시 처리하는지 확인합니다.

### 5단계: 경계가 포함되는지 결정하기

특히 이진 탐색에서 다음을 코드보다 먼저 정합니다.

```text
[low, high] 인가?
[low, high) 인가?
mid는 다음 탐색에서 다시 볼 필요가 있는가?
```

### 6단계: 샘플 외 반례 네 개 실행하기

```text
최소 크기
최대 또는 큰 값
답이 없는 경우
현재 코드의 핵심 조건을 깨는 경우
```

## 8. 제출 전 1분 체크리스트

- [ ] `dp[i]` 또는 `distance[i]`의 의미를 말할 수 있는가?
- [ ] 초기값이 상태 정의와 일치하는가?
- [ ] 가장 작은 입력에서 존재하지 않는 인덱스를 읽지 않는가?
- [ ] 이미 방문한 정점을 다시 큐나 힙에 넣지 않는가?
- [ ] Union-Find에서 루트끼리 합치는가?
- [ ] 힙의 부모-자식 불변식을 삽입과 삭제 모두 복구하는가?
- [ ] 그래프 방향과 간선 가중치를 문제와 동일하게 저장했는가?
- [ ] 고립 정점, 중복 간선, 도달 불가능한 상태를 처리하는가?
- [ ] `INF`가 실제 가능한 답과 충돌하지 않는가?
- [ ] `solution()`의 반환과 `print`가 한 번만, 올바른 형식으로 동작하는가?
- [ ] 샘플 외에 최소 입력과 반례를 확인했는가?
- [ ] 최악의 시간·공간 복잡도가 입력 제한 안에 들어오는가?

## 9. 발전 방향

### 우선순위 1: 새 문제보다 기존 문제 재검증하기

당분간은 새로운 알고리즘을 넓히기보다, 이미 푼 문제의 코드를 다음 방식으로 다시 확인하는 것이 효과적입니다.

1. 문제마다 상태/불변식을 한 문장으로 적습니다.
2. 첨삭에서 발견된 반례를 직접 실행합니다.
3. 반례를 하나 더 스스로 만듭니다.
4. 왜 그 반례에서 틀리는지 코드 한 줄로 설명합니다.

### 우선순위 2: 알고리즘별 대표 불변식을 암기하기

공식을 외우기보다 다음 문장을 익히세요.

- DP: 현재 상태에 필요한 이전 상태가 모두 계산되어 있다.
- BFS: 큐에 들어간 순간 최단 거리가 확정되며 다시 넣지 않는다.
- 다익스트라: 음이 아닌 가중치에서 가장 짧은 후보를 꺼낸 순간 그 거리를 확정할 수 있다.
- 위상 정렬: 진입 차수 0은 남은 선행 작업이 없다는 뜻이다.
- 크루스칼: 서로 다른 집합을 연결할 때만 간선을 선택한다.
- 이진 탐색: 한쪽 구간은 가능, 다른 쪽 구간은 불가능이라는 경계를 유지한다.
- 힙: 부모보다 작은 값이 아래에 남지 않도록 위/아래로 복구한다.

### 우선순위 3: 반례 제작 능력 키우기

특히 다음 조합을 의도적으로 만들어 보세요.

- 첫 번째 값은 크고 두 번째 값은 작은 DP 입력
- 목표보다 큰 동전
- 고립된 그래프 정점
- 방향을 뒤집으면 결과가 달라지는 간선
- 중복 간선
- 사이클이 있는 그래프
- 빈 힙, 원소 1~3개인 힙
- `low`, `high`, `mid`가 붙어 탐색이 끝나기 어려운 이진 탐색 입력

## 마무리

현재 가장 큰 강점은 **알고리즘의 본질을 빠르게 파악하는 것**입니다. 가장 큰 보완점은 **그 알고리즘이 성립하기 위한 조건을 코드 끝까지 보존하는 것**입니다.

다음 문제부터는 “무슨 알고리즘이지?” 다음에 바로 “초기 상태와 불변식은 무엇이지?”, “최소 입력에서 그대로 실행되나?”, “출력 형식까지 맞나?”를 묻는 습관을 들이면 됩니다. 이 세 질문만 꾸준히 적용해도 현재 반복되는 오답의 상당 부분을 줄일 수 있습니다.
